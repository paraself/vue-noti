{
  "name": "vue-noti",
  "version": "2.0.0",
  "description": "A Vue Notification plugin which fallback to web notification api when page visibility is off.",
  "main": "index.js",
  "scripts": {
    "build": "npx tsc -p .",
    "watch": "npx tsc -p . -w",
    "dev": "npx tsc -p . -w",
    "clean": "rm -rf node_modules && npm cache clean --force",
    "test": "vue serve ./example/index.ts -o"
  },
  "author": "Yves Wang",
  "repository": {
    "type": "git",
    "url": "https://github.com/paraself/vue-noti"
  },
  "license": "ISC",
  "peerDependencies": {
    "vue": "^2.5.0",
    "vue-notification": "^1.3.16"
  },
  "devDependencies": {
    "typescript": "^3.5.3",
    "vue": "^2.6.10"
  }
}
